import { BarChartIcon } from "@radix-ui/react-icons";

const Icon = () => {
  return (
    <div className="flex items-center gap-2">
      <BarChartIcon />
      <p>Resound Shop</p>
    </div>
  );
};

export default Icon;
